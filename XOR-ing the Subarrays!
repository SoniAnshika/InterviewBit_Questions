int Solution::solve(vector<int> &A) {
    int n=A.size();
    
    if(n%2==0)
        return 0;
    else
    {
        int xorr=0;
        for(int i=0;i<n;i++)
        {
            if(i%2==0)
                xorr^=A[i];
        }
         return xorr;
    }
    
}
