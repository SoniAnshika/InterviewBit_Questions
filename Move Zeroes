vector<int> Solution::solve(vector<int> &A) {
    
    int j=0;
    
    for(int i=0;i<A.size();i++)
    {
        if(A[i]!=0)
        {
            A[j]=A[i];
            j++;
        }
    }
    while(j<A.size())
    {
        A[j]=0;
        j++;
    }
    
    return A;
}
